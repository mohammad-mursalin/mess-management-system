services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: mess_manager
      POSTGRES_USER: mess_admin
      POSTGRES_PASSWORD: choose-a-local-password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: